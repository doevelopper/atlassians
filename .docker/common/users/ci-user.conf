# Default CI user settings.
# These values are consumed by .docker/common/scripts/setup-user.sh
# and can be overridden via Dockerfile ARG / --build-arg.
CI_USER=ci
CI_UID=1001
CI_GID=1001
CI_HOME=/home/ci
CI_SHELL=/bin/bash
